# Tic-Tac-Toe Game - Improved Version

## 🎮 เกี่ยวกับโปรเจค

เกม Tic-Tac-Toe ฉบับปรับปรุงพิเศษ สำหรับ LAB6 - JavaScript DOM Manipulation

## ✨ ฟีเจอร์ที่เพิ่มเติม

### ✅ ฟีเจอร์ตามโจทย์ LAB6

1. **เปลี่ยนสีของ X และ O**
   - X = สีแดง (#e74c3c)
   - O = สีน้ำเงิน (#3498db)
   - ใช้ CSS filter เพื่อเปลี่ยนสีรูปภาพ

2. **ปุ่ม Reset เพื่อเริ่มเกมส์ใหม่**
   - ปุ่มสีเขียวด้านล่างกระดาน
   - รีเซ็ตกระดานแต่ยังคงคะแนนไว้

3. **เก็บคะแนนการชนะของแต่ละฝ่าย**
   - Scoreboard แสดงด้านบน
   - อัพเดทอัตโนมัติเมื่อมีผู้ชนะ

### 🎨 ฟีเจอร์พิเศษเพิ่มเติม

- ✅ ไฮไลท์ช่องที่ชนะด้วยสีเหลืองและ animation pulse
- ✅ แสดงสถานะเกมปัจจุบัน (ผู้เล่นคนไหนเล่น, ใครชนะ, หรือเสมอ)
- ✅ Animation fadeIn เมื่อวางเครื่องหมาย
- ✅ Hover effect บนช่องว่าง
- ✅ Responsive design สำหรับหน้าจอเล็ก
- ✅ Gradient background สวยงาม
- ✅ Modern UI/UX

## 📁 โครงสร้างไฟล์

```
tictactoe-improved/
├── index.html      # โครงสร้าง HTML
├── style.css       # การจัดรูปแบบและ animations
├── script.js       # Logic ของเกม
└── README.md       # เอกสารนี้
```

## 🚀 วิธีใช้งาน

1. **เปิดไฟล์**
   ```bash
   # เปิด index.html ในเบราว์เซอร์
   open index.html  # macOS
   start index.html # Windows
   xdg-open index.html # Linux
   ```

2. **วิธีเล่น**
   - Player X เริ่มเล่นก่อน
   - คลิกช่องว่างเพื่อวางเครื่องหมาย
   - เกมจะตรวจสอบผู้ชนะอัตโนมัติ
   - ชนะ 3 ช่องในแนวเดียวกัน (แนวนอน, แนวตั้ง, หรือแนวทแยง)

3. **Reset เกม**
   - กดปุ่ม "🔄 Reset Game" เพื่อเริ่มเกมใหม่
   - คะแนนจะยังคงอยู่

## 💻 โค้ดสำคัญ

### 1. การเปลี่ยนสี X และ O (CSS)

```css
/* เปลี่ยนสีของ X และ O */
#grid div.x-player img {
    filter: hue-rotate(0deg) saturate(150%) brightness(0.8);
    /* สีแดงสำหรับ X */
}

#grid div.o-player img {
    filter: hue-rotate(180deg) saturate(150%) brightness(1.2);
    /* สีน้ำเงินสำหรับ O */
}
```

### 2. การตรวจสอบผู้ชนะ (JavaScript)

```javascript
// รูปแบบการชนะทั้งหมด (8 แบบ)
const winningConditions = [
    [0, 1, 2], // แถวบน
    [3, 4, 5], // แถวกลาง
    [6, 7, 8], // แถวล่าง
    [0, 3, 6], // คอลัมน์ซ้าย
    [1, 4, 7], // คอลัมน์กลาง
    [2, 5, 8], // คอลัมน์ขวา
    [0, 4, 8], // เส้นทแยงซ้าย-ขวา
    [2, 4, 6]  // เส้นทแยงขวา-ซ้าย
];

// วนตรวจสอบทุกรูปแบบ
for (let i = 0; i < winningConditions.length; i++) {
    const [a, b, c] = winningConditions[i];
    if (gameBoard[a] === gameBoard[b] && gameBoard[b] === gameBoard[c]) {
        // มีผู้ชนะ!
    }
}
```

### 3. การเก็บคะแนน (JavaScript)

```javascript
// เพิ่มคะแนนเมื่อชนะ
if (currentPlayer === 'X') {
    scoreX++;
    scoreXDisplay.textContent = scoreX;
} else {
    scoreO++;
    scoreODisplay.textContent = scoreO;
}
```

### 4. ฟังก์ชัน Reset (JavaScript)

```javascript
function resetGame() {
    currentPlayer = 'X';
    gameBoard = ['', '', '', '', '', '', '', '', ''];
    gameActive = true;
    
    boxes.forEach(box => {
        box.innerHTML = '';
        box.classList.remove('x-player', 'o-player', 'filled', 'winner');
    });
}
```

## 🎯 การทำงาน

### Flow ของเกม

1. **เริ่มเกม**: Player X เล่นก่อน
2. **คลิกช่อง**: 
   - ตรวจสอบว่าช่องว่าง
   - เพิ่มรูปภาพ X หรือ O
   - เพิ่ม class เพื่อเปลี่ยนสี
3. **ตรวจสอบผล**:
   - มีผู้ชนะ? → แสดงข้อความและเพิ่มคะแนน
   - เสมอ? → แสดงข้อความเสมอ
   - ไม่มี → เปลี่ยนผู้เล่น
4. **Reset**: เริ่มเกมใหม่แต่เก็บคะแนนไว้

### ตัวแปรสำคัญ

- `currentPlayer`: เก็บว่าตอนนี้ผู้เล่นคนไหน ('X' หรือ 'O')
- `gameBoard`: array เก็บสถานะกระดาน 9 ช่อง
- `gameActive`: boolean เช็คว่าเกมยังเล่นอยู่หรือไม่
- `scoreX`, `scoreO`: เก็บคะแนนของแต่ละฝ่าย

## 🎨 CSS Variables

```css
:root {
    --color-x: #e74c3c;      /* สีแดงสำหรับ X */
    --color-o: #3498db;      /* สีน้ำเงินสำหรับ O */
    --color-winner: #f1c40f; /* สีเหลืองสำหรับช่องชนะ */
}
```

## 📱 Responsive Design

เกมจะปรับขนาดอัตโนมัติตามหน้าจอ:
- Desktop: กระดาน 450x450px
- Mobile: กระดาน 300x300px

## 🔧 เทคโนโลジีที่ใช้

- **HTML5**: โครงสร้างหน้าเว็บ
- **CSS3**: 
  - Flexbox สำหรับ layout
  - CSS Variables สำหรับสี
  - Animations และ Transitions
  - CSS Filter สำหรับเปลี่ยนสีรูปภาพ
- **JavaScript (ES6)**:
  - DOM Manipulation
  - Event Listeners
  - Array และ Object operations
  - Conditional logic

## ✅ เปรียบเทียบก่อน-หลังการปรับปรุง

### เดิม (Original)
- ❌ เล่นได้เฉพาะ X
- ❌ ไม่มีการตรวจสอบผู้ชนะ
- ❌ ไม่มีการเก็บคะแนน
- ❌ ไม่มีปุ่ม Reset
- ❌ ไม่มีการแสดงสถานะ

### ใหม่ (Improved)
- ✅ เล่นสลับกัน X และ O
- ✅ ตรวจสอบผู้ชนะอัตโนมัติ
- ✅ เก็บคะแนนการชนะ
- ✅ มีปุ่ม Reset
- ✅ แสดงสถานะเกมปัจจุบัน
- ✅ เปลี่ยนสี X (แดง) และ O (น้ำเงิน)
- ✅ ไฮไลท์ช่องที่ชนะ
- ✅ Animations และ Effects สวยงาม

## 🎓 สิ่งที่เรียนรู้

1. **DOM Manipulation**:
   - การเลือก elements ด้วย querySelector
   - การสร้าง elements ใหม่
   - การเพิ่ม/ลบ class

2. **Event Handling**:
   - การจัดการ click events
   - การใช้ event.currentTarget

3. **Game Logic**:
   - การเก็บ state ของเกม
   - การตรวจสอบเงื่อนไขการชนะ
   - การสลับผู้เล่น

4. **CSS Techniques**:
   - CSS Variables
   - CSS Filter effects
   - Animations และ Transitions
   - Flexbox layout

## 🐛 การแก้ปัญหา

### ปัญหา 1: รูปภาพไม่เปลี่ยนสี
**วิธีแก้**: ใช้ CSS `filter` property แทนการเปลี่ยนไฟล์รูปภาพ

### ปัญหา 2: คลิกช่องซ้ำได้
**วิธีแก้**: เช็คว่า `gameBoard[index]` ว่างหรือไม่ก่อนเพิ่มรูปภาพ

### ปัญหา 3: Reset ไม่ทำงาน
**วิธีแก้**: ใช้ `innerHTML = ''` เพื่อลบรูปภาพทั้งหมด

## 📚 การพัฒนาต่อ

ไอเดียสำหรับฟีเจอร์เพิ่มเติม:
- [ ] เพิ่มโหมด AI (เล่นกับคอมพิวเตอร์)
- [ ] เพิ่มเสียงเอฟเฟกต์
- [ ] บันทึกคะแนนใน localStorage
- [ ] เพิ่มโหมด 2 players online
- [ ] เพิ่มประวัติการเล่น
- [ ] เพิ่มธีมสีต่างๆ

## 📄 License

สร้างขึ้นเพื่อการศึกษาในรายวิชา ENGCE301 - Web Application Development

---

**สร้างโดย**: Claude AI  
**วันที่**: 7 มกราคม 2026  
**สำหรับ**: LAB6 - JavaScript DOM Manipulation